/** Quest XP penalties — profile XP via addXP only. */

export const QUEST_PENALTY_XP = {
  dailyExpired: -25,
  dailyFailed: -12,
  dailyIncompleteDenied: -18,
  weeklyExpired: -40,
  weeklyFailed: -20,
  weeklyIncompleteDenied: -28,
}
